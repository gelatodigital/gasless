{
  "name": "@gelatocloud/gasless",
  "private": true,
  "author": "Gelato",
  "description": "Gelato Gasless SDK: All-in-one solution for gasless transactions",
  "homepage": "https://github.com/gelatodigital/gasless",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "types:check": "tsc --noEmit",
    "biome:changed": "biome check --changed",
    "biome:staged": "biome check --staged --write",
    "biome:check": "biome check --write",
    "biome:format": "biome format --write",
    "prepare": "lefthook install",
    "changeset:publish": "pnpm build && changeset publish",
    "changeset:version": "changeset version && pnpm format",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@changesets/cli": "2.29.7",
    "@types/node": "latest",
    "lefthook": "2.0.2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=23"
  },
  "packageManager": "pnpm@10.15.0",
  "pnpm": {
    "overrides": {
      "@gelatocloud/gasless": "workspace:*"
    },
    "peerDependencyRules": {
      "allowedVersions": {
        "@gelatocloud/gasless": "*"
      }
    },
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "lefthook"
    ]
  }
}
